# 물어봐이브 (Mureo-Vibe) - FSD 폴더 구조

## 📁 프로젝트 구조

```
mooro-vibe/
├── src/
│   ├── app/                    # 애플리케이션 레벨
│   │   ├── styles/            # 전역 스타일
│   │   └── providers/         # 전역 Provider (Context, Theme 등)
│   │
│   ├── pages/                  # 페이지 레벨
│   │   └── main/              # 메인 페이지
│   │
│   ├── widgets/                # 위젯 레벨 (복합 UI 블록)
│   │   └── answer-display/    # AI 답변 표시 위젯
│   │
│   ├── features/               # 기능 레벨
│   │   ├── question-input/    # 질문 입력 기능
│   │   └── ai-answer/         # AI 답변 생성 기능
│   │
│   └── shared/                 # 공유 레벨
│       ├── ui/                # 공통 UI 컴포넌트 (Button, Input 등)
│       ├── api/               # API 통신 로직
│       ├── lib/               # 유틸리티 함수
│       └── config/            # 설정 파일
│
├── .env                        # 환경 변수 (gitignore됨)
├── .prettierrc                 # Prettier 설정
├── .gitignore                  # Git ignore 설정
└── package.json                # 프로젝트 의존성

```

## 🎯 각 레이어 설명

### App Layer (app/)
- 애플리케이션의 전역 설정과 초기화 코드
- 전역 스타일, Provider, Router 등

### Pages Layer (pages/)
- 라우팅되는 페이지 컴포넌트
- 각 페이지는 widgets와 features를 조합

### Widgets Layer (widgets/)
- 복잡한 UI 블록 (여러 features를 포함할 수 있음)
- 비즈니스 로직은 최소화

### Features Layer (features/)
- 사용자 시나리오별 기능
- 비즈니스 로직 포함

### Shared Layer (shared/)
- 프로젝트 전체에서 재사용되는 코드
- UI 컴포넌트, API, 유틸리티 등

## 🔐 환경 변수

프로젝트 루트에 `.env` 파일을 생성하고 다음 내용을 추가하세요:

```
VITE_GEMINI_API_KEY=your_api_key_here
```

## 🚀 시작하기

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm run dev

# 빌드
npm run build
```

